# Normalize line endings so Windows/other tools stop producing phantom diffs.
# After committing this file once, run:  git add --renormalize .  && commit.
* text=auto eol=lf
*.ps1 text eol=crlf
*.pdf binary
*.png binary
*.jpg binary
